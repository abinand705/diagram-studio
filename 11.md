You are a synthetic data factory. For each of the following 13 diagram types, produce a JSON array of 5 training examples (text-to-diagram). Each example must have "instruction" and "output" (valid Mermaid). Use the exact Mermaid syntax shown for each type. Return a single JSON object with keys being the diagram type names and values being the arrays. Diagram types and their Mermaid examples are:

Class Diagram:
classDiagram
class User {
  +String name
  +String email
  +login(): bool
}
class Order {
  +int id
  +Date date
  +calcTotal(): float
}
User "1" -- "0..*" Order : places

Object Diagram:
objectDiagram
object User1 {
  name = "Alice"
  email = "alice@example.com"
}
object Order1 {
  id = 101
  date = 2025-01-10
}
User1 -- Order1 : places

Component Diagram:
componentDiagram
component Frontend {
  port apiIn
}
component Backend {
  port apiOut
}
apiIn -- apiOut

Deployment Diagram:
deploymentDiagram
node WebServer {
  artifact FrontendApp
}
node DBServer {
  artifact Database
}
WebServer -- DBServer : TCP/IP

Package Diagram:
packageDiagram
package "UI Layer" {
  package Web {
    component LoginPage
  }
}
package "Domain Layer" {
  package Services {
    component UserService
  }
}
Web --> UserService

Use Case Diagram:
usecaseDiagram
actor User
User --> (Login)
User --> (View Dashboard)
(Login) ..> (Validate Credentials) : <<include>>

Sequence Diagram:
sequenceDiagram
actor User
User->>Frontend: submitLogin()
Frontend->>Backend: POST /auth
Backend-->>Frontend: token
Frontend-->>User: dashboard.html

Activity Diagram:
activityDiagram
start
if (User logged in?) then (yes)
  :Show Dashboard;
else (no)
  :Show Login;
endif
stop

State Machine Diagram:
stateDiagram
[*] --> Idle
Idle --> Processing : start
Processing --> Done : finish
Done --> [*]

Communication Diagram:
communicationDiagram
actor User
User -> WebServer : login()
WebServer -> Database : validateUser()
Database --> WebServer : result
WebServer --> User : dashboard

ER Diagram:
erDiagram
CUSTOMER {
  int customer_id PK
  string name
}
ORDER {
  int order_id PK
  int customer_id FK
  date order_date
}
CUSTOMER ||--o{ ORDER : places

DFD (Data Flow Diagram):
flowchart LR
    A[Customer] -->|order details| B((Process Order))
    B -->|validated order| C[(Order Store)]
    B -->|receipt| A

Context Diagram:
flowchart TD
    User([User]) -->|queries| System[[My System]]
    System -->|results| User
    Admin([Admin]) -->|config| System

Output structure:
{
  "Class Diagram": [ { "instruction":..., "output":... }, ... ],
  "Object Diagram": [ ... ],
  ...
}