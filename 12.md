You are working on a React application that uses React Flow to render ER diagrams.

Your task is to improve the diagram rendering by implementing a proper layout engine and cleaner ER visualization.

Follow these instructions carefully:

--------------------------------------------------
1. INSTALL AND USE DAGRE FOR AUTO LAYOUT
--------------------------------------------------

- Install the dagre library
- Use it to automatically position nodes in a left-to-right layout (rankdir: "LR")
- Ensure nodes do not overlap and have proper spacing

Implementation requirements:

- Create a utility function called `getLayoutedElements(nodes, edges)`
- Use dagre.graphlib.Graph()
- Set node width ≈ 180 and height ≈ 80
- After layout, update each node's position using dagre output

--------------------------------------------------
2. CONVERT ER STRUCTURE TO CLEAN NODE FORMAT
--------------------------------------------------

- Do NOT use separate shapes for attributes (no ovals)
- Each entity should be a single node (table-style)

Each node must display:
- Entity name (bold)
- Divider line
- List of attributes
- Highlight primary key (e.g., prefix with 🔑 or bold)

Example node data:

{
  label: "Customer",
  attributes: ["id", "name", "email"],
  primary_key: "id"
}

--------------------------------------------------
3. RENDER RELATIONSHIPS AS EDGES (NO DIAMOND SHAPES)
--------------------------------------------------

- Do NOT create separate nodes for relationships
- Use edges instead

Each edge must:
- Connect source entity → target entity
- Display label (e.g., "places")
- Optionally display cardinality (1:N, etc.)

--------------------------------------------------
4. IMPROVE EDGE STYLE
--------------------------------------------------

- Use smoothstep or bezier edges
- Add arrow markers
- Style edges with:
  - strokeWidth: 2
  - color: #333

--------------------------------------------------
5. ENSURE CLEAN SPACING
--------------------------------------------------

- Horizontal spacing between entities: ~250px
- Vertical spacing: ~150px
- Avoid overlapping edges and nodes

--------------------------------------------------
6. INTEGRATE WITH EXISTING AI OUTPUT
--------------------------------------------------

- Assume AI returns JSON with:
  - entities
  - relationships

Convert that JSON into:
- React Flow nodes
- React Flow edges

Then pass them through the layout function before rendering

--------------------------------------------------
7. FINAL GOAL
--------------------------------------------------

The diagram should:
- Look clean and readable
- Automatically arrange entities
- Show attributes inside nodes
- Show relationships as labeled edges
- Avoid overlaps

--------------------------------------------------

Return the full updated React code including:
- dagre setup
- layout function
- node transformation logic
- edge transformation logic
- integration with React Flow

Do not explain. Just implement clean, working code.